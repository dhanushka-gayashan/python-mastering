# List
grades = [80, 75, 90, 100]

total = sum(grades)
length =  len(grades)
average = total / length
print(average)


# Tuple
grades = (80, 75, 90, 100)

total = sum(grades)
length = len(grades)
average = total / length
print(average)


# Set
grades = {80, 75, 90, 100}

total = sum(grades)
length = len(grades)
average = total / length
print(average)
